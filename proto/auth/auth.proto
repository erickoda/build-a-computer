syntax = "proto3";

package auth;

message AuthRequest {
    string email = 1;
    string password = 2;
}

message AuthReply {
    string token = 1;
}

service Auth {
    rpc AuthenticateUser (AuthRequest) returns (AuthReply);
}
